allprojects { afterEvaluate { project -> if (project.name == 'image_picker_android') { def task = project.tasks.findByName('compileReleaseJavaWithJavac'); println('task=' + task); if (task != null) { try { println('classpath=' + task.classpath.files.size()); task.classpath.files.take(20).each { println('cp:' + it) } println('bootClasspath=' + task.bootClasspath); println('source=' + task.source.files.size()); task.source.files.take(20).each { println('src:' + it) } } catch(Throwable t) { println('taskErr=' + t); } } } } }
