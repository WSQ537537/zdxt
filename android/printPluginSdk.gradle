allprojects { afterEvaluate { project -> if (project.name == 'image_picker_android') { println('PLUGIN_ROOT=' + project.rootDir); println('PROJECT_DIR=' + project.projectDir); println('ROOT_LOCAL=' + project.rootProject.file('local.properties').exists()); println('ROOT_LOCAL_PATH=' + project.rootProject.file('local.properties').path); println('ANDROID_HOME=' + System.getenv('ANDROID_HOME')); println('ANDROID_SDK_ROOT=' + System.getenv('ANDROID_SDK_ROOT')); def androidExt = project.extensions.findByName('android'); println('HAS_ANDROID_EXT=' + (androidExt != null)); if (androidExt != null) { try { println('compileSdk=' + androidExt.compileSdk); } catch(Exception e) { println('compileSdkErr=' + e); } } } } }
